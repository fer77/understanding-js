// DEFAULT VALUES

//function greet(name) {
//    console.log(name);
//    console.log('Hello ' + name);
//}

//greet('Fernando');


//agreet();
//* The + operator will coerse undefined to a string and return 'Hello undefined'.

//* in the console:
//0 || 1
//* returns 1 It returns whatever value coerses to true.
//undefined || 'hello'
//* returns "hello" because it is coersed to true.  That is the || special behavior, it returns whatever value coerses to true first.
//Boolean("hello")
//* returns true

//* Setting a default value:
//function greet(name) {
//    name = name || '<your name here>'; //* Order of operation will run || first and then the = sign. If name is true it will return true otherwise it'll return the default.
//    console.log('Hello ' + name);
//}

//greet('Fernando');
//greet();


